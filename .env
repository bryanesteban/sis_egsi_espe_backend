# ====================================
# Variables de Entorno - SIEGSI
# ====================================
# 
# Copia este archivo a `.env` y ajusta los valores según tu entorno
# Uso: docker-compose --env-file .env up -d

# === Base de Datos MySQL ===
DB_HOST=mysql-db
DB_PORT=3306
DB_NAME=db_egsi
DB_USERNAME=usuario
DB_PASSWORD=sa12345
MYSQL_ROOT_PASSWORD=rootpassword

# === Backend Spring Boot ===
SPRING_PROFILES_ACTIVE=docker
JAVA_OPTS=-Xmx512m -Xms256m

# === Seguridad ===
# ⚠️ IMPORTANTE: Cambiar JWT_SECRET en producción
JWT_SECRET=TuClaveSecretaMuyLargaParaLaTesisDeBryan2025ProductionKey
JWT_EXPIRATION=86400000

# === CORS ===
# Lista de orígenes permitidos separados por coma
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:9090

# === Configuración Regional ===
TZ=America/Guayaquil

# === Puertos Expuestos ===
# Backend
BACKEND_PORT=9090
# MySQL (puerto externo para evitar conflictos)
MYSQL_PORT=3307
